import { Galaxy } from '../../../app/server/models/Galaxy';

describe('has a facebookId ', () => {
  let galaxy;
  beforeEach(() => {
    galaxy = new Galaxy();
  });
  it('has a facebookId ', () => {});
});
