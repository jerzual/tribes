import { Player } from '../../../app/server/models/Player';

describe('has a facebookId ', () => {
  let player;
  beforeEach(() => {
    player = new Player();
  });
  it('has a facebookId ', () => {});
});
