import Board from './Board';

describe('Board', () => {

    let board;

    beforeEach(() => {
        board = new Board();
    });

    it('has a set of tiles', () => {
        expect(true).toBe(true);
    });

});
